<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Data</title>
    <style>
      /* Styling dasar */
      body {
        max-width: 1200px;
        margin: auto;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f8f9fa;
        color: #333;
        line-height: 1.6;
        padding: 20px;
      }

      h1 {
        text-align: center;
        color: #4caf50;
        margin-bottom: 20px;
      }

      form {
        display: grid;
        grid-template-columns: repeat(4, 1fr); /* 4 kolom dalam satu baris */
        gap: 20px; /* Jarak antar elemen */
        background: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .form-group {
        display: flex;
        flex-direction: column;
      }

      label {
        font-weight: bold;
        margin-bottom: 5px;
      }

      input[type="text"],
      input[type="number"] {
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 14px;
      }

      /* Styling untuk tombol */
      button {
        grid-column: span 4; /* Tombol memakan seluruh baris */
        padding: 10px 20px;
        font-size: 16px;
        color: white;
        background-color: #4caf50;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #45a049;
      }

      a {
        margin-top: 20px;
        display: inline-block;
        text-align: center;
        color: #4caf50;
        text-decoration: none;
        font-weight: bold;
      }

      a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <h1>Tambah Data Mahasiswa</h1>
    <form action="/update/<%= data.id %>" method="POST">
      <!-- Baris pertama -->
      <div class="form-group">
        <label for="nim">NIM:</label>
        <input
          type="text"
          id="nim"
          name="nim"
          required
          value="<%= data.nim %>"
        />
      </div>
      <div class="form-group">
        <label for="nama">Nama:</label>
        <input
          type="text"
          id="nama"
          name="nama"
          required
          value="<%= data.nama %>"
        />
      </div>
      <div class="form-group">
        <label for="prodi">Prodi:</label>
        <input
          type="text"
          id="prodi"
          name="prodi"
          required
          value="<%= data.prodi %>"
        />
      </div>
      <div class="form-group">
        <label for="semester">Semester:</label>
        <input
          type="number"
          id="semester"
          name="semester"
          required
          value="<%= data.semester %>"
        />
      </div>

      <!-- Baris kedua -->
      <div class="form-group">
        <label for="nilai_presensi">Nilai Presensi:</label>
        <input
          type="number"
          id="nilai_presensi"
          name="nilai_presensi"
          step="0.01"
          required
          value="<%= data.nilai_presensi %>"
        />
      </div>
      <div class="form-group">
        <label for="nilai_tugas">Nilai Tugas:</label>
        <input
          type="number"
          id="nilai_tugas"
          name="nilai_tugas"
          step="0.01"
          required
          value="<%= data.nilai_tugas %>"
        />
      </div>
      <div class="form-group">
        <label for="nilai_uts">Nilai UTS:</label>
        <input
          type="number"
          id="nilai_uts"
          name="nilai_uts"
          step="0.01"
          required
          value="<%= data.nilai_uts %>"
        />
      </div>
      <div class="form-group">
        <label for="nilai_uas">Nilai UAS:</label>
        <input
          type="number"
          id="nilai_uas"
          name="nilai_uas"
          step="0.01"
          required
          value="<%= data.nilai_uas %>"
        />
      </div>

      <!-- Tombol Simpan -->
      <button type="submit">Simpan</button>
    </form>
    <a href="/">Kembali ke Home</a>
  </body>
</html>
